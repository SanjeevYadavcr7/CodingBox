#include<bits/stdc++.h>
using namespace std;

int rob(TreeNode* root) {
        
}

int main(){
    int n; 
    cin >> n;
    vector<int> arr(n,0);
    for(int i=0; i<n; i++) cin >> arr[i];
 
 
    return 0;
}