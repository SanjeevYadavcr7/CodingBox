#include<bits/stdc++.h>
using namespace std;

vector<vector<int> > findDistance(vector<vector<char> >& matrix, int M, int N){ 
    
} 

int main(){
    int n; 
    cin >> n;
    vector<int> arr(n,0);
    for(int i=0; i<n; i++) cin >> arr[i];
 
 
    return 0;
}