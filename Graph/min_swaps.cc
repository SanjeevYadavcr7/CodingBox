#include<bits/stdc++.h>
using namespace std;

int minSwaps(vector<int> &arr1){

}

int main(){
	int n;
    cin>>n;
    vector<int> arr(n);
    for(int i=0;i<n;i++) cin>>arr[i];
   
    cout<<minSwaps(arr);
	
	return 0;
}
